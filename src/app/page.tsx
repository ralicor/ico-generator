import React from "react";
import IconGenerator from "./(components)/(IconGenerator)/component";

const HomePage = () => {
  return (
    <div>
      <IconGenerator />
    </div>
  );
};

export default HomePage;
